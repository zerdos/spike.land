{
  "name": "@spike.land/code-worker",
  "description": "An edge chat service that runs on Cloudflare Workers using Durable Objects",
  "version": "0.6.15",
  "module": "./dist/index.mjs",
  "scripts": {
    "deploy": "wrangler publish",
    "bundle": "esbuild --bundle --format=esm --loader:.html=text --target=esnext src/index.ts --outfile=dist/index.mjs",
    "postpublish": "yarn && yarn bundle"
  },
  "devDependencies": {
    "@spike.land/cf-npm-site": "^0.6.1",
    "@spike.land/code": "^0.6.11"
  },
  "private": true,
  "dependencies": {
    "@types/websocket": "1.0.4",
    "textdiff-patch": "1.0.5"
  }
}
