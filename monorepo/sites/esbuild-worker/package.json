{
  "name": "esbuild-worker",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --outdir=dist ./src/index.ts",
    "dev": "miniflare dist/index.js --watch --debug --https --open"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "3.11.0",
    "esbuild": "^0.14.43",
    "miniflare": "^2.5.0"
  },
  "dependencies": {
    "typescript": "^4.7.3"
  }
}
