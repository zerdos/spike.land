{
  "name": "@spike.land/api",
  "description": "An edge chat service that runs on Cloudflare Workers using Durable Objects",
  "version": "0.8.0",
  "module": "./public/api.mjs",
  "scripts": {
    "build": "esbuild --bundle --format=esm --target=esnext src/index.ts --outfile=public/index.mjs"
  },
  "private": true
}
