{
  "name": "monorepo",
  "version": "0.9.56",
  "description": "our monorepo",
  "repository": "git@github.com:zerdos/spike.land.git",
  "author": "Zoltan Erdos <zolika84@gmail.com>",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "devcontainers/*"
  ],
  "scripts": {
    "dev": "yarn workspace @spike-npm-land/code dev",
    "dev:local": "concurrently --kill-others-on-fail \"yarn workspace @spike-npm-land/code run build:vite:watch\" \"cd packages/testing.spike.land && sleep 8 && yarn dev\"",
    "dev:fe": "yarn workspace @spike-npm-land/code dev",
    "dev:workers": "yarn workspaces foreach --all -p --include '@spike-npm-land/{code-worker,transpile}' run dev",
    "build:all": "yarn workspaces foreach --all -p run build",
    "build:fe": "cd packages/code && yarn build",
    "build:workers": "yarn workspaces foreach --all -p --include '@spike-npm-land/{code-worker,transpile}' run build",
    "deploy:dev": "NODE_ENV=development yarn build:fe && cd packages/testing.spike.land && yarn w:deploy:dev",
    "types:check": "tsc -b -f && yarn lint",
    "types:build": "yarn workspaces foreach --all -p run types:build",
    "test": "NODE_OPTIONS='--no-warnings' vitest --reporter dot --silent --no-color run",
    "test:e2e": "playwright test tests/*.spec.ts",
    "ci": "yarn install --immutable && yarn lint && yarn types:check && yarn test:ci && yarn test:chat:cucumber",
    "ci:check": "echo '🔧 Running complete CI checks...' && yarn install --immutable && echo '✅ Dependencies installed' && yarn eslint --max-warnings=200 && echo '✅ ESLint passed' && yarn types:check && echo '✅ TypeScript check passed' && cd packages/code && yarn vitest run && cd .. && echo '✅ Unit tests passed' && cd chat && npx playwright install chromium && yarn test:cucumber && cd ../.. && echo '✅ E2E tests passed' && yarn build:all && echo '✅ Build successful' && echo '🎉 All CI checks passed!'",
    "ci:quick": "yarn eslint --max-warnings=200 && yarn types:check && yarn test",
    "ci:docker": "docker-compose -f docker-compose.ci.yml up --build --abort-on-container-exit",
    "test:ci": "cd packages/code && yarn vitest run && cd ../chat && npx playwright install chromium && yarn test:cucumber",
    "test:chat:cucumber": "cd packages/chat && yarn test:cucumber",
    "clean": "yarn workspaces foreach --all -p run clean",
    "lint": "eslint --config eslint.config.js . --cache --max-warnings=200",
    "lint:fix": "eslint --config eslint.config.js . --fix --cache",
    "fmt": "yarn dprint fmt \"packages/**/*\"",
    "release": "yarn clean && yarn build:all && yarn deploy:full",
    "docker:build": "yarn workspaces foreach --all -p run docker:build",
    "docker:push": "yarn workspaces foreach --all -p run docker:push"
  },
  "devDependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/sheet": "^1.4.0",
    "@eslint/eslintrc": "3.3.1",
    "@eslint/js": "9.36.0",
    "@mui/styled-engine": "^7.3.2",
    "@playwright/test": "^1.55.1",
    "@types/aria-query": "^5.0.4",
    "@types/d3-path": "^3.1.1",
    "@types/d3-time": "^3.0.4",
    "@types/events": "^3.0.3",
    "@types/node": "24.10.4",
    "@types/react": "^19.2.7",
    "@typescript-eslint/eslint-plugin": "^8.44.1",
    "@typescript-eslint/parser": "^8.44.1",
    "@vitest/utils": "^3.2.4",
    "concurrently": "9.2.1",
    "d3-path": "^3.1.0",
    "d3-time": "^3.1.0",
    "deep-eql": "^5.0.2",
    "dprint": "0.50.2",
    "eslint": "9.36.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "execa": "^9.6.0",
    "jsdom": "^27.3.0",
    "lightningcss": "^1.30.1",
    "pathe": "^2.0.3",
    "playwright": "^1.55.1",
    "react": "^19.2.3",
    "supports-color": "^10.2.2",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  },
  "packageManager": "yarn@4.12.0"
}
