{
  "name": "zedvision",
  "description": "our monorepo",
  "repository": "git@github.com:zed-vision/monorepo.git",
  "author": "Zoltan Erdos <zolika84@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "serve": "yarn copy && file_server --cert .certs/_wildcard.zed.dev.pem --key .certs/_wildcard.zed.dev-key.pem --host cms.zed.dev docs",
    "push": "yarn build && git add . && git commit -m pre-release && git push && git pull",
    "release": "cd packages/code && yarn build && cd ../smart-monaco-editor && yarn build && cd ../.. && yarn push && lerna publish patch --yes && yarn push",
    "build": "cd packages/diff && yarn build && cd ../../ && yarn lerna run build --parallel --since b && yarn copy",
    "clean": "yarn lerna run clean --parallel --since b",
    "copy": "rm -rf docs && mkdir -p docs/code && cp -a packages/zed-vision-site/public/. docs/ && cp -a packages/code/. docs/code/ && cp -a packages/diff/. docs/diff/ && rm -rf docs/code/.certs",
    "rebuild": "yarn lerna run clean --parallel && yarn  build && yarn copy",
    "docker:build": "yarn lerna run docker:build --parallel",
    "docker:push": "yarn docker:build && yarn lerna run docker:push --parallel"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lerna run build --parallel --since b"
    }
  },
  "dependencies": {
    "@babel/standalone": "7.12.10",
    "@emotion/react": "11.1.2",
    "@emotion/styled": "11.0.0",
    "@types/uuid": "8.3.0",
    "diff": "5.0.0",
    "framer-motion": "2.9.5",
    "idb": "5.0.8",
    "monaco-editor": "0.21.2",
    "normalize.css": "8.0.1",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "uuid": "8.3.2"
  },
  "devDependencies": {
    "@emotion/cache": "11.0.0",
    "@emotion/utils": "1.0.0",
    "@types/prop-types": "15.7.3",
    "@types/react": "17.0.0",
    "@types/react-dom": "17.0.0",
    "csstype": "3.0.5",
    "husky": "5.0.4",
    "lerna": "3.22.1",
    "popmotion": "9.0.1",
    "typescript": "4.1.3"
  }
}
