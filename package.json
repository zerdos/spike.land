{
  "name": "github.com-spike.land",
  "description": "our monorepo",
  "repository": "git@github.com:spike-land/monorepo.git",
  "author": "Zoltan Erdos <zoliika84@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "dockerhub/devimages/*",
    "sites/*"
  ],
  "scripts": {
    "start": "yarn lerna build && yarn open && yarn serve",
    "__hard__reformat": "rm -rf node_modules monorepo &&  git reset --hard HEAD && deno fmt . && yarn && yarn build:prod",
    "open": "open-cli https://localhost:8000/monorepo/packages/code/js/index.html",
    "clean": "yarn lerna  clean",
    "serve": "nodemon ./http2-server/http2-server.mjs",
    "push": "NODE_ENV=production yarn lerna push",
    "importmap": "yarn lerna importmap",
    "build:code": "yarn lerna build:code",
    "deploy": "yarn lerna run deploy",
    "lerna:publish": "yarn lerna lerna:publish",
    "push:prod": "NODE_ENV=production yarn build-and-push",
    "build-and-push": "yarn importmap && yarn re:build && yarn lerna push:prod",
    "re:build": "NODE_ENV=production  yarn lerna run re:build",
    "build:all": "yarn lerna run re:build",
    "build:prod": "NODE_ENV=production yarn build:all",
    "docker:build": "yarn lerna docker:build",
    "docker:push": "yarn lerna docker:push",
    "postinstall": "yarn build:prod",
    "lerna": "lerna",
    "open-cli": "open-cli",
    "pw": "playwright",
    "test": "echo \"Error: no test specified\" && exit 1"
},
"dependencies": {
  "@cloudflare/workers-types": "3.11.0",
  "@jspm/generator": "1.0.0-beta.31",
  "lerna": "5.1.1",
  "typescript": "4.7.3",
  "wrangler": "2.0.9"
}
}