{
  "private": false,
  "name": "@zedvision/spike-land-counter",
  "version": "14.5.4",
  "publishConfig": {
    "access": "public"
  },
  "description": "A template for kick starting a Cloudflare Workers project",
  "module": "./dist/index.mjs",
  "scripts": {
    "build": "esbuild --bundle --format=esm --target=esnext src/index.mjs --outfile=dist/index.mjs",
    "deploy": "wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "2.2.2",
    "@cloudflare/wrangler": "1.19.3"
  },
  "dependencies": {
    "esbuild": "^0.13.3",
    "is-even": "^1.0.0"
  }
}
