{
  "name": "@zedvision/ipfs",
  "version": "13.0.2",
  "sideEffects": false,
  "files": [
    "*/*"
  ],
  "keywords": [
    "ipfs"
  ],
  "authors": [
    "Zoltan Erdos <zolika84@gmail.com>"
  ],
  "scripts": {
    "clean": "rm -rf dist && yarn types && yarn types && yarn build",
    "types": "yarn tsc -d",
    "build:client": "yarn esbuild  src/ipfs.client.ts --bundle --platform=browser --minify --format=esm --outfile=dist/ipfs.client.js",
    "prebuild": "yarn build:client && cp -f dist/ipfs.client.js ../code/modules/ipfs.client.js",
    "build": "echo done"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zed-vision/monorepo"
  },
  "homepage": "https://github.com/zed-vision/monorepo",
  "repository": "https://github.com/zed-vision/monorepo",
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "12815f92581c029760fde4f16e6a1e724795545a",
  "devDependencies": {
    "esbuild": "0.8.45",
    "ipfs-message-port-client": "0.5.2",
    "it-all": "1.0.5",
    "typescript": "4.1.5",
    "uint8arrays": "2.1.2"
  }
}
