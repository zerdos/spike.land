{
  "name": "@spike.land/code-worker",
  "description": "An edge chat service that runs on Cloudflare Workers using Durable Objects",
  "version": "0.9.2",
  "main": "dist/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "yarn deploy:dev",
    "deploy:dev": "wrangler publish --env=testing src/index.ts",
    "prepublishOnly": "wrangler publish",
    "predeploy": "yarn c:build && yarn x:build",
    "c:build": "yarn workspace @spike.land/code build",
    "x:build": "esbuild  --bundle --tree-shaking=true --metafile=good.json --format=esm --loader:.html=text --loader:.ico=binary --external:__STATIC_CONTENT_MANIFEST --tree-shaking --platform=browser src/index.ts --outdir=dist"
  },
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "0.2.0",
    "esbuild": "0.15.12",
    "typescript": "4.8.4",
    "wrangler": "2.1.13"
  },
  "dependencies": {
    "@cloudflare/workers-types": "3.18.0",
    "@spike.land/code": "*",
    "avl": "1.5.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "db8e23b83d2dd82efc101eca9d1b5ec76d122df4"
}
