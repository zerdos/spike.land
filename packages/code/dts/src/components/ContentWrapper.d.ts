export declare const ContentWrapper: ({ children, scale, innerHeight, width, bgColor, rgba, type }: {
  children: JSX.Element;
  scale: number;
  innerHeight: number;
  width: number;
  bgColor: number[];
  rgba: (r: number, g: number, b: number, a: number) => string;
  type: string;
}) => import("@emotion/react/jsx-runtime").JSX.Element;
