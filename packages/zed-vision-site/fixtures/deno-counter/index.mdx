---
title: My Second Post!
date: "2020-10-06T23:46:37.121Z"
description: "deno couner example coding"
---

So lets try something new.
I will use deno, and vanilla typescript to develop an easy counter app.

<hr />

# h1 
## h2 
### h3
#### h4 
##### h5 


*This text will be italic*




I think you should use an `<addr>` element here instead.


**This text will be bold**


- [x] @mentions, #refs, [links](), **formatting**, and <del>tags</del> supported
- [x] list syntax required (any unordered or ordered list supported)
- [x] this is a complete item
- [ ] this is an incomplete item


```javascript live=true
//
//  Counter.tsx
//

() => {
    const [state, setState] = React.useState({counter: 0})
    
    const actions = {
    increase: ()=>setState({...state, counter: state.counter + 1}),
    }
    
    return <h3 onClick={actions.increase}>
        Counter: {state.counter}
        </h3>
}
```

As Kanye West said:

> We're living the future so
> the present is our past.

_You **can** combine them_


# h1 

## h2 ```code```

text


```var i=code```
 