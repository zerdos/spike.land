{
  "name": "@zedvision/emotion-react-renderer",
  "version": "11.9.5",
  "main": "src/index.js",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "source": "./src/renderer.ts",
  "exports": "./renderer",
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf dist .tsbuildinfo && deno fmt src",
    "build:renderer": "esbuild src/renderer.ts --minify --bundle --format=esm --platform=browser  --define:process.env.NODE_ENV=\\\"production\\\"  --tree-shaking=ignore-annotations --target=esnext --outfile=dist/bundle.js",
    "build:draggableWindow": "esbuild src/DraggableWindow.tsx --minify --bundle --format=esm --platform=browser  --define:process.env.NODE_ENV=\\\"production\\\"  --tree-shaking=ignore-annotations --target=esnext --outfile=dist/draggableWindowBundle.js",
    "prebuild": "yarn build:renderer && yarn build:draggableWindow",
    "build": "echo done in prebuld"
  },
  "devDependencies": {
    "@types/react": "17.0.0",
    "@types/react-dom": "17.0.0",
    "esbuild": "0.8.32",
    "typescript":"4.2.0-beta"
  },
  "dependencies": {
    "@emotion/react": "11.1.4",
    "framer-motion": "3.2.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
