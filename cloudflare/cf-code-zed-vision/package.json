{
  "private": true,
  "name": "@zedvision/cf-code-zed-vision",
  "version": "11.10.6",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "deno fmt src",
    "build": "esbuild --bundle src/index.ts --target=esnext --platform=browser --outfile=dist/index.js",
    "deploy": "yarn build && wrangler publish"
  },
  "author": "Zoltan Erdos <zoltan.erdos@me.com>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/wrangler": "1.13.0",
    "@zedvision/ipfs": "^11.10.6",
    "esbuild": "0.8.33"
  },
  "dependencies": {
    "@zedvision/code": "^11.10.6",
    "cids": "^1.1.5"
  }
}
