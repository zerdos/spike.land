{
  "private": true,
  "name": "@zedvision/cf-code-zed-vision",
  "version": "13.0.6",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "deno fmt src",
    "build": "rm -rf dist && yarn esbuild --bundle src/codeZedVisionHandler.ts --target=es2018 --platform=browser --outfile=dist/index.js",
    "deploy": "yarn build && wrangler publish"
  },
  "author": "Zoltan Erdos <zoltan.erdos@me.com>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "2.1.0",
    "@cloudflare/wrangler": "1.13.0",
    "@zedvision/ipfs": "^13.0.6",
    "esbuild": "0.8.54"
  },
  "dependencies": {
    "@zedvision/code": "^13.0.6"
  }
}
